import sys
import Fonctions

t = open(sys.argv[1])

liste = []

for ligne in t :
    f = ligne.split()
    liste.append(f)

liste = Fonctions.CleanVertical(liste)
liste = Fonctions.CleanHorizontal(liste)
"""Fonctions.OffsetChecker(liste)"""

Ethernet = []
IPv4 = []
TCP = []
Fonctions.Extract(liste,Ethernet,IPv4,TCP)

for i in Ethernet:
    print(i)
for i in IPv4:
    print(i)
for i in TCP:
    print(i)






